"""Pydantic request/response shapes — empty by design until routes exist.

Routes define their consumers; schemas follow. Don't build shapes in
isolation; each schema belongs to the route that serves it.
"""
