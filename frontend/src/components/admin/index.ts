/**
 * Admin section primitives (#187). Co-located here because they're
 * specific to admin UI patterns; promote to `components/ui/` if a
 * non-admin call site ever appears.
 */

export { ActionSection } from './ActionSection';
export { ConfirmButton } from './ConfirmButton';
export { IdInput } from './IdInput';
export { ScopeRadios, type Scope } from './ScopeRadios';
